# Workflow UI Moved

The workflow is now rendered in the React app at [/workflow](../frontend/src/pages/WorkflowPage.jsx) through the new dynamic diagram page.

Open the page in the frontend and you’ll get the clickable SVG workflow instead of this markdown version.
